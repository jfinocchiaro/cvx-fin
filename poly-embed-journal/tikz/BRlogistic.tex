%\documentclass{standalone}
%\usepackage{tikz}
%\usepackage{pgfplots}

%\begin{document}[scale=\tikzfigscalepgf, thick]
\begin{tikzpicture}[scale=\tikzfigscalepgf, thick]
    \begin{axis}[
        axis lines = left,
        xlabel = $p$,
        ylabel = Bayes risk $\underline L^{logistic}(p)$,
        xmin = 0,
        xmax = 1,
        ymin = 0,
        ymax =  1,
        grid = none,
        height=5cm, width=6cm,
    ]
    % Plot the function
    \addplot[
        domain=0.001:0.999,
        samples=100,
        color=blue,
        thick
    ]
    {x * ln(1 + exp(-ln(x / (1-x)))) + (1-x) * ln(1+exp(ln(x / (1-x))))};
    \end{axis}
    \node at (2.2,4){\tiny{Bayes risk for logistic loss}};
\end{tikzpicture}
%\end{document}
