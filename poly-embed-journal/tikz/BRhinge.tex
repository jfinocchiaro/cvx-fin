%\documentclass{standalone}
%\usepackage{tikz}
%\usepackage{pgfplots}

%\begin{document}
\begin{tikzpicture}[scale=\tikzfigscalepgf, thick]
    \begin{axis}[
        axis lines = left,
        xlabel = $p$,
        ylabel = Bayes risk $\underline L^{hinge}(p)$,
        xmin = 0,
        xmax = 1,
        ymin = 0,
        ymax =  1,
        grid = none,
        height=5cm, width=6cm,
    ]
    % Plot the function
    \addplot[
        domain=0.001:0.999,
        samples=100,
        color=blue,
        thick
    ]
    {2 * min(x, 1-x)};
    \end{axis}
    \node at (2.2,4){\tiny{Bayes risk for hinge loss}};
\end{tikzpicture}
%\end{document}
